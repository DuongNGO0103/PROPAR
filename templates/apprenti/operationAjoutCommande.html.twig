{% extends 'base.html.twig' %}

{% block title %}Dashboard
{% endblock %}

{% block navbarlists %}
	<lu>
		<a href="{{ path('app_apprenti') }}">Tableau de bord</a>
		<br/><br/>
		<a href="{{ path('app_apprenti_operations') }}">Ajouter une opération</a>
		<br/><br/>
		<a href="{{ path('app_liste_operations') }}">Lister mes opérations</a>
		<br/><br/>
		
	</lu>

{% endblock %}

{% block main2 %}
	<h2>Confirmer la commande</h2>
	{{ form_start(form) }}
	{{ form_row(form.nomCommande, {"attr": {"class": "btn disabled"}})}}
	{{ form_row(form.date, {"attr": {"class": "btn disabled"}})}}
	{{ form_row(form.statut)}}
	{{ form_row(form.client, {"attr": {"class": "btn disabled"}})}}
	{{ form_row(form.operation)}}
	{{ form_row(form.user,{'attr': {'value': app.user.username }})}}
	<button type="submit" class="buttonValiderRegister">Confirmer</button>
	{{ form_end(form) }}

{% endblock %}
